apiVersion: v1
kind: Service
metadata:
  name: {{ include "fake-news-detector.fullname" . }}
  labels:
    {{- include "fake-news-detector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fake-news-detector.selectorLabels" . | nindent 4 }}
